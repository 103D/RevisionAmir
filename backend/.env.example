# Backend Environment Variables
# Для локального запуска (Vercel использует свои переменные)

APP_HOST=0.0.0.0
APP_PORT=8000

# Redis URL (опционально, для локального Redis)
# Пример: REDIS_URL=redis://localhost:6379
# Если не задано, используются JSON-файлы в backend/data/
REDIS_URL=
